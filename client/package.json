{
	"//": "Internal sub-package: the language client's own dependencies and build. It is never published — the extension manifest is the package.json at the repository root, and that one owns the version the Marketplace sees. The version below is not kept in step with it, and nothing reads it.",
	"name": "vscode-regelspraak-client",
	"description": "VS Code language client for RegelSpraak",
	"license": "Apache-2.0",
	"private": true,
	"version": "0.0.1",
	"engines": {
		"vscode": "^1.101.0"
	},
	"dependencies": {
		"glob": "^11.0.0",
		"vscode-languageclient": "^9.0.1"
	},
	"//@types/vscode": "Pinned, and deliberately not a range: it is the compile-time VS Code API surface, so anything newer than the oldest host we claim to support (engines.vscode) would type-check here and be undefined for a user. Keep it equal to the engines.vscode floor, and raise it only together with that floor.",
	"devDependencies": {
		"@types/mocha": "^10.0.6",
		"@types/vscode": "1.101.0",
		"@vscode/test-electron": "^2.3.9",
		"mocha": "^11.8.0"
	},
	"overrides": {
		"diff": "^8.0.4",
		"serialize-javascript": "^7.0.5"
	}
}
